# Canonical runtime mirror for `[project.dependencies]` in pyproject.toml.
# Optional dashboard / ML / regime / metrics extras live in requirements-optional.txt.

python-dotenv>=1.0.0
polars>=1.39.0
aiohttp>=3.9.0
numpy>=1.26.0
aiogram>=3.27.0
python-binance>=1.0.22
websockets>=14.0,<16.0
aiosqlite>=0.22.1
msgspec>=0.19.0
tenacity>=9.0.0
structlog>=24.1.0
pydantic>=2.0.0

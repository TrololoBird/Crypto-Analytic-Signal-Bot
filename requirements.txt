# Dependency manifest for crypto-signal-bot (Python 3.14.5).
#
# Canonical source of truth: pyproject.toml
# Reproducible pins:        requirements-lock.txt
# Import smoke test:        python scripts/verify_dependencies.py
#
# Recommended installs:
#   Minimal runtime (bot without dashboard/regime ML):
#     pip install -e .
#
#   Production live bot (dashboard, faster JSON, polars_ta):
#     pip install -e ".[live]"
#
#   Development (lint, typecheck, hooks, tests):
#     pip install -e ".[dev,test]"
#
#   Full local workstation:
#     pip install -e ".[live,regime,dev,test]"
#
# ---------------------------------------------------------------------------
# CORE RUNTIME  (imported on `python main.py` hot path)
# ---------------------------------------------------------------------------
# python-dotenv   -> bot/secrets.py
# polars          -> features, strategies, market data, tracking
# aiohttp         -> bot/market/rest_*, delivery/messaging
# numpy           -> features*, regime helpers, setups/smc
# aiogram         -> bot/delivery/telegram.py
# websockets      -> bot/market/ws_*
# aiosqlite       -> repository, migrations, diary_store
# msgspec         -> domain/events, tracked_signals
# structlog       -> logging_config, features, messaging
# pydantic        -> domain/config.py (2.13.x; <2.14 until validated)
# tenacity        -> bot/delivery/telegram.py retries
#
# ---------------------------------------------------------------------------
# LIVE EXTRA  [live]
# ---------------------------------------------------------------------------
# fastapi, uvicorn, prometheus-client, orjson, polars_ta, polars-ols
#
# ---------------------------------------------------------------------------
# REGIME EXTRA  [regime]  — optional; ImportError fallback in bot/regime/
# ---------------------------------------------------------------------------
# hmmlearn, scikit-learn, statsmodels
#
# ---------------------------------------------------------------------------
# DEV / TEST
# ---------------------------------------------------------------------------
# ruff==0.15.15, mypy>=2.1.0, pre-commit, pytest, pytest-asyncio, pytest-cov
#
# ---------------------------------------------------------------------------
# Editable install (default for developers)
# ---------------------------------------------------------------------------

-e .[live,dev,test]

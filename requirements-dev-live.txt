# Dev / test / live-check dependency layer.
# Install on top of runtime dependencies:
#   pip install -r requirements.txt -r requirements-dev-live.txt
# Source of truth: pyproject optional extras [dev], [test], [live], [regime], [ml].

# Quality & typing
ruff>=0.15.12
mypy>=1.20.2
pre-commit>=4.6.0

# Tests
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
respx>=0.23.1
aioresponses>=0.7.8

# Live checks / operational extras
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
prometheus-client>=0.25.0
orjson>=3.11.8
polars_ta>=0.5.17

# Optional regime modeling
hmmlearn>=0.3.3
scikit-learn>=1.8.0

# Optional ML
joblib>=1.5.3
lightgbm>=4.6.0
xgboost>=3.2.0
optuna>=4.8.0

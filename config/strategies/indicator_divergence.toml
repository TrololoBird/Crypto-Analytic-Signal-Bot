# indicator_divergence — catalog defaults (ops-tunable)

[strategy]
enabled = true
name = "indicator_divergence"

[scoring]
base_score = 0.53

[filters]
min_volume_ratio = 0.75
min_adx = 15.0

[risk_management]
sl_buffer_atr = 0.85
min_rr = 1.9

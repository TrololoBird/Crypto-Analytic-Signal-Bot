"""Shared kernel: market data plane, feature pipeline, domain vocabulary, transports.

Imported by both the main bot (bot/) and the hunter (hunt).
MUST NOT depend on the `bot` or `hunt` packages.
"""

# Archived hunt scripts (P11)

Dev-only split/cutover helpers kept for reference. Runtime entry:

```bash
cd hunt && PYTHONPATH=. python -m hunt_core watch
```

Root repo shims: `scripts/dump_minute_watch.py`, `scripts/hunt_scanner.py`, `scripts/hunt_signal.py`.
